---
config:
  flowchart:
    curve: linear
---
graph TD;
	__start__([<p>__start__</p>]):::first
	intent_router(intent_router)
	collect(collect)
	plan(plan)
	search_realtime(search_realtime)
	execute_select(execute_select)
	process_payment(process_payment)
	summary(summary)
	check_weather(check_weather)
	side_chat(side_chat)
	guide(guide)
	__end__([<p>__end__</p>]):::last
	__start__ --> intent_router;
	check_weather --> guide;
	collect -.-> __end__;
	collect -.-> plan;
	intent_router -.-> check_weather;
	intent_router -.-> collect;
	intent_router -.-> execute_select;
	intent_router -.-> plan;
	intent_router -.-> process_payment;
	intent_router -.-> search_realtime;
	intent_router -.-> side_chat;
	plan --> guide;
	process_payment -.-> guide;
	process_payment -.-> summary;
	side_chat --> guide;
	execute_select --> __end__;
	guide --> __end__;
	search_realtime --> __end__;
	summary --> __end__;
	classDef default fill:#f2f0ff,line-height:1.2
	classDef first fill-opacity:0
	classDef last fill:#bfb6fc
