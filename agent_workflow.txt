---
config:
  flowchart:
    curve: linear
---
graph TD;
	__start__([<p>__start__</p>]):::first
	intent_router(intent_router)
	collect(collect)
	plan(plan)
	search_flight(search_flight)
	select_flight(select_flight)
	pay_flight(pay_flight)
	search_hotel(search_hotel)
	select_hotel(select_hotel)
	pay_hotel(pay_hotel)
	summary(summary)
	check_weather(check_weather)
	side_chat(side_chat)
	guide(guide)
	__end__([<p>__end__</p>]):::last
	__start__ --> intent_router;
	check_weather --> guide;
	collect -.-> __end__;
	collect -.-> plan;
	intent_router -.-> check_weather;
	intent_router -.-> collect;
	intent_router -.-> pay_flight;
	intent_router -.-> pay_hotel;
	intent_router -.-> plan;
	intent_router -.-> search_flight;
	intent_router -.-> search_hotel;
	intent_router -.-> select_flight;
	intent_router -.-> select_hotel;
	intent_router -.-> side_chat;
	pay_flight -.-> guide;
	pay_flight -.-> search_hotel;
	pay_hotel -.-> guide;
	pay_hotel -.-> summary;
	plan --> guide;
	side_chat --> guide;
	guide --> __end__;
	search_flight --> __end__;
	search_hotel --> __end__;
	select_flight --> __end__;
	select_hotel --> __end__;
	summary --> __end__;
	classDef default fill:#f2f0ff,line-height:1.2
	classDef first fill-opacity:0
	classDef last fill:#bfb6fc
